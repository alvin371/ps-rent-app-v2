import type { StationRuntime } from "./types";

export const initialStations: StationRuntime[] = [];

export const menuTabs: { label: string; tone: string }[] = [];

export const menuItems: {
  id: string;
  name: string;
  price: number;
  label: string;
  tone: string;
}[] = [];
